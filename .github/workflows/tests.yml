...
- name: 📦 Install Composer dependencies
  uses: ramsey/composer-install@v3
  # Hoặc lệnh run: composer install --no-dev --prefer-dist (tùy thuộc vào cách bạn cài đặt)

- name: 💾 Setup Database (Run Migrations)
  run: |
    # Lệnh này sẽ tạo các bảng cần thiết, bao gồm bảng "category"
    php artisan migrate --force
    
- name: 🏃 Run Tests (PHPUnit)
  run: vendor/bin/phpunit
...

name: Tests

on:
  push:
    branches:
      - master
      - '*.x'
  pull_request:
  schedule:
    - cron: '0 0 * * *'

permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: true
      matrix:
        php: [8.2, 8.3, 8.4]

    name: PHP ${{ matrix.php }}

    ...
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # ... (Các bước Setup PHP, Install Composer dependencies, Copy environment file, Generate app key giữ nguyên) ...

      - name: Generate app key
        run: php artisan key:generate

      # 🚨 BƯỚC MỚI: Thiết lập và Chạy Migration
      - name: Setup Database (SQLite In-Memory) and Migrate
        # Thiết lập biến môi trường cho bước này
        env:
          DB_CONNECTION: sqlite 
          DB_DATABASE: ':memory:' # Buộc sử dụng DB trong bộ nhớ
        run: |
          # Chạy migration để tạo các bảng
          php artisan migrate --force

      # 🚨 BƯỚC THAY ĐỔI: Chạy kiểm thử với biến môi trường
      - name: Execute tests
        # Thiết lập biến môi trường cho bước này để nó dùng cùng DB trong bộ nhớ
        env:
          DB_CONNECTION: sqlite
          DB_DATABASE: ':memory:'
        run: php artisan test
